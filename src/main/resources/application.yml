spring:
  application:
  name: inflearn-spring-ai-study
  config:
    import: optional:file:.env[.properties] # config env file using


logging:
  level:
    root: INFO

# LLM SERVICE
llm:
  gpt:
    key: ${GPT_API_KEY}